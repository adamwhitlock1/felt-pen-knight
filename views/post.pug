extends layout.pug

block content
	div.content
		p Hello #{user.name}!
		form(method='post', action='frames/new/null', enctype='multipart/form-data')
			input(type='file', name='image')
			input(type='submit')
